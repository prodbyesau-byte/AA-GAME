{
  "name": "aa-game",
  "version": "1.0.0",
  "description": "Anders Andersen window cleaner browser game prototype.",
  "private": true,
  "main": "src/main.ts",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "dev:test": "vite --host 127.0.0.1 --port 5173 --open /?test=1",
    "editor": "vite --host 127.0.0.1 --port 5173 --open /level-editor.html",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "phaser": "^3.90.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
